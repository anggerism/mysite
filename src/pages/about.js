import React from "react"
import Layout from "../components/Layout"

//set up component and export as default
const about = () => {
  return (
    <Layout>
      <div className="about-heading">
        <h1>about</h1>
      </div>
    </Layout>
  )
}

export default about
